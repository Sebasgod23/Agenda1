specVersion: "v1beta"
serviceId: "pwa-re"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "pwa-re-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
